# Task 12 hierarchical planner fixtures

These table fixtures are represented directly in `src-tauri/tests/hierarchical_route_planner.rs`.
They cover fixed hard-gate order, Primary/ConfiguredBackup/DepletedEmergency strata,
PriorityFirst, CostFirst, bounded candidate count, and affinity not restoring rejected routes.
